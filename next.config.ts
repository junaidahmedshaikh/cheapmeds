import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
   images: {
    domains: ['5.imimg.com',
      'www.ashcroftpharmacy.co.uk',
      'www.hlhealthcare.co.in',
    'www.saphnixlifecare.com',
  ], // <-- Add the domain here
  },
};

export default nextConfig;
